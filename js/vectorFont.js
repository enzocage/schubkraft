export const VECTOR_FONT = {
  'A': [2,8, 4,0,  4,0, 6,8,  3,5, 5,5],
  'B': [2,0, 2,8,  2,0, 5,0,  5,0, 6,2,  6,2, 5,4,  2,4, 5,4,  5,4, 6,6,  6,6, 5,8,  5,8, 2,8],
  'C': [6,2, 4,0,  4,0, 2,2,  2,2, 2,6,  2,6, 4,8,  4,8, 6,6],
  'D': [2,0, 2,8,  2,0, 5,0,  5,0, 6,2,  6,2, 6,6,  6,6, 5,8,  5,8, 2,8],
  'E': [2,0, 2,8,  2,0, 6,0,  2,4, 5,4,  2,8, 6,8],
  'F': [2,0, 2,8,  2,0, 6,0,  2,4, 5,4],
  'G': [6,2, 4,0,  4,0, 2,2,  2,2, 2,6,  2,6, 4,8,  4,8, 6,8,  6,8, 6,4,  6,4, 4,4],
  'H': [2,0, 2,8,  6,0, 6,8,  2,4, 6,4],
  'I': [4,0, 4,8,  2,0, 6,0,  2,8, 6,8],
  'J': [6,0, 6,6,  6,6, 4,8,  4,8, 2,8,  2,8, 2,5],
  'K': [2,0, 2,8,  2,4, 6,0,  2,4, 6,8],
  'L': [2,0, 2,8,  2,8, 6,8],
  'M': [2,8, 2,0,  2,0, 4,4,  4,4, 6,0,  6,0, 6,8],
  'N': [2,8, 2,0,  2,0, 6,8,  6,8, 6,0],
  'O': [3,0, 5,0,  5,0, 7,2,  7,2, 7,6,  7,6, 5,8,  5,8, 3,8,  3,8, 1,6,  1,6, 1,2,  1,2, 3,0],
  'P': [2,0, 2,8,  2,0, 5,0,  5,0, 6,2,  6,2, 6,4,  6,4, 5,4,  5,4, 2,4],
  'Q': [3,0, 5,0,  5,0, 7,2,  7,2, 7,6,  7,6, 5,8,  5,8, 3,8,  3,8, 1,6,  1,6, 1,2,  1,2, 3,0,  5,5, 7,8],
  'R': [2,0, 2,8,  2,0, 5,0,  5,0, 6,2,  6,2, 6,4,  6,4, 5,4,  5,4, 2,4,  4,4, 6,8],
  'S': [6,2, 5,0,  5,0, 3,0,  3,0, 2,2,  2,2, 6,6,  6,6, 5,8,  5,8, 3,8,  3,8, 2,6],
  'T': [4,0, 4,8,  2,0, 6,0],
  'U': [2,0, 2,6,  2,6, 4,8,  4,8, 6,6,  6,6, 6,0],
  'V': [2,0, 4,8,  4,8, 6,0],
  'W': [2,0, 2,8,  2,8, 4,4,  4,4, 6,8,  6,8, 6,0],
  'X': [2,0, 6,8,  6,0, 2,8],
  'Y': [2,0, 4,4,  6,0, 4,4,  4,4, 4,8],
  'Z': [2,0, 6,0,  6,0, 2,8,  2,8, 6,8],
  '0': [2,0, 6,0,  6,0, 6,8,  6,8, 2,8,  2,8, 2,0,  6,0, 2,8],
  '1': [3,2, 4,0,  4,0, 4,8,  2,8, 6,8],
  '2': [2,2, 3,0,  3,0, 5,0,  5,0, 6,2,  6,2, 2,8,  2,8, 6,8],
  '3': [2,0, 6,0,  6,0, 4,4,  4,4, 6,4,  6,4, 6,6,  6,6, 5,8,  5,8, 2,8],
  '4': [5,8, 5,0,  5,0, 2,5,  2,5, 6,5],
  '5': [6,0, 2,0,  2,0, 2,4,  2,4, 5,4,  5,4, 6,5,  6,5, 6,7,  6,7, 5,8,  5,8, 2,8],
  '6': [5,0, 3,0,  3,0, 2,2,  2,2, 2,7,  2,7, 4,8,  4,8, 6,7,  6,7, 6,4,  6,4, 2,4],
  '7': [2,0, 6,0,  6,0, 3,8],
  '8': [3,0, 5,0,  5,0, 6,2,  6,2, 5,4,  5,4, 3,4,  3,4, 2,2,  2,2, 3,0,  3,4, 2,6,  2,6, 3,8,  3,8, 5,8,  5,8, 6,6,  6,6, 5,4],
  '9': [2,4, 5,4,  5,4, 6,2,  6,2, 4,0,  4,0, 2,1,  2,1, 2,4,  6,1, 6,6,  6,6, 4,8,  4,8, 2,8],
  ' ': [],
  '.': [4,7, 4,8],
  ',': [4,7, 3,8],
  ':': [4,2, 4,3,  4,6, 4,7],
  '-': [2,4, 6,4],
  '+': [4,2, 4,6,  2,4, 6,4],
  '/': [2,8, 6,0],
  '\\': [2,0, 6,8],
  '!': [4,0, 4,5,  4,7, 4,8],
  '?': [2,2, 4,0,  4,0, 6,2,  6,2, 4,4,  4,4, 4,6,  4,8, 4,8],
  '(': [4,0, 2,2,  2,2, 2,6,  2,6, 4,8],
  ')': [2,0, 4,2,  4,2, 4,6,  4,6, 2,8],
  '>': [2,1, 6,4,  6,4, 2,7],
  '<': [6,1, 2,4,  2,4, 6,7],
  '=': [2,3, 6,3,  2,5, 6,5],
  '_': [2,8, 6,8],
  '★': [4,0, 5,3,  8,3, 5,5,  6,8, 4,6,  2,8, 3,5,  0,3, 3,3],
  '♪': [5,0, 5,6,  5,0, 7,1,  7,1, 7,3,  3,6, 5,6,  3,6, 3,8,  3,8, 5,8,  5,8, 5,6],
  '—': [1,4, 7,4]
};

export function drawVectorChar(ctx, char, x, y, scaleX, scaleY, color) {
  const segments = VECTOR_FONT[char.toUpperCase()] || VECTOR_FONT['?'];
  ctx.strokeStyle = color;
  ctx.beginPath();
  for (let i = 0; i < segments.length; i += 4) {
    const px1 = x + segments[i] * scaleX;
    const py1 = y + segments[i+1] * scaleY;
    const px2 = x + segments[i+2] * scaleX;
    const py2 = y + segments[i+3] * scaleY;
    ctx.moveTo(px1, py1);
    ctx.lineTo(px2, py2);
  }
  ctx.stroke();
}

export function drawVectorText(ctx, text, x, y, size, color) {
  const spacing = size * 10;
  for (let i = 0; i < text.length; i++) {
    drawVectorChar(ctx, text[i], x + i * spacing, y, size, size, color);
  }
}

// Animated typography: per-letter sine wave bounce with optional
// rainbow shimmer and scale pulse. opts: {amp, speed, phase, rainbow, pulse}
export function drawWavyText(ctx, text, x, y, size, color, opts = {}) {
  const spacing = size * 10;
  const t = Date.now() * (opts.speed !== undefined ? opts.speed : 0.005);
  const amp = opts.amp !== undefined ? opts.amp : size * 2.2;
  const phaseStep = opts.phase !== undefined ? opts.phase : 0.55;
  for (let i = 0; i < text.length; i++) {
    const ph = t + i * phaseStep;
    const dy = Math.sin(ph) * amp;
    const s = size * (opts.pulse ? 1 + Math.sin(ph * 1.3) * 0.07 : 1);
    let col = color;
    if (opts.rainbow) {
      const hue = (i * 26 + Date.now() * 0.05) % 360;
      col = `hsl(${hue}, 100%, 68%)`;
    }
    drawVectorChar(ctx, text[i], x + i * spacing, y + dy - (s - size) * 4, s, s, col);
  }
}

// Typewriter reveal: shows the first `progress * text.length` chars
// plus a blinking block cursor at the write position.
export function drawTypewriterText(ctx, text, x, y, size, color, progress) {
  const spacing = size * 10;
  const shown = Math.floor(Math.max(0, Math.min(1, progress)) * text.length);
  for (let i = 0; i < shown; i++) {
    drawVectorChar(ctx, text[i], x + i * spacing, y, size, size, color);
  }
  if (shown < text.length && Math.floor(Date.now() / 160) % 2 === 0) {
    ctx.save();
    ctx.fillStyle = color;
    ctx.fillRect(x + shown * spacing + size, y, size * 6, size * 8);
    ctx.restore();
  }
}

// Per-letter drop-in with overshoot bounce. elapsed in seconds since
// the animation started; letters cascade with a small stagger.
export function drawDropInText(ctx, text, x, y, size, color, elapsed, opts = {}) {
  const spacing = size * 10;
  const stagger = opts.stagger !== undefined ? opts.stagger : 0.06;
  const dur = opts.dur !== undefined ? opts.dur : 0.45;
  for (let i = 0; i < text.length; i++) {
    const p = Math.max(0, Math.min(1, (elapsed - i * stagger) / dur));
    if (p <= 0) continue;
    // ease-out back (slight overshoot)
    const c = 1.7;
    const e = 1 + (c + 1) * Math.pow(p - 1, 3) + c * Math.pow(p - 1, 2);
    const dy = (1 - e) * -46;
    ctx.save();
    ctx.globalAlpha *= Math.min(1, p * 2.5);
    drawVectorChar(ctx, text[i], x + i * spacing, y + dy, size, size, color);
    ctx.restore();
  }
}
